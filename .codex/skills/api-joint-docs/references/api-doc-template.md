# 接口联调文档

## 1. 元信息

- 接口名称：待确认
- 所属模块：待确认
- 变更类型：新增 / 更新 / 废弃 / 待确认
- 文档状态：草案 / 待确认 / 已确认

## 2. 请求

- Method：GET / POST / PUT / PATCH / DELETE / 待确认
- Path：`/api/...`
- Content-Type：`application/json` / `multipart/form-data` / 待确认
- Auth：需要 / 不需要 / 待确认
- 幂等性：是 / 否 / 待确认

## 3. 调用时机

- 触发场景 / 调用方动作：待确认
- 调用前置条件：待确认
- 调用顺序 / 依赖：待确认
- 调用频率、重试或轮询：不适用 / 待确认

## 4. 参数

| 字段 | 位置 | 类型 | 必填 | 约束 | 示例 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| 待确认 | path / query / header / cookie / body | 待确认 | 是 / 否 | 待确认 | 待确认 | 待确认 |

## 5. 响应

### 成功响应

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| code | number | 业务状态码 |
| message | string | 业务提示 |
| data | object | 响应数据 |

### 错误响应

| 状态码 | 业务码 | 触发条件 | 调用方处理 |
| --- | --- | --- | --- |
| 400 | 待确认 | 参数校验失败 | 展示校验提示 |
| 401 | 待确认 | 未登录或 token 失效 | 跳转登录或刷新 token |

### 错误示例

#### 示例：参数校验失败

```http
HTTP/1.1 400 Bad Request
Content-Type: application/json

{
  "code": "待确认",
  "message": "待确认",
  "data": null
}
```

交付文档必须将示例中的 `待确认` 替换为已核实值；如果存在不同的错误响应结构，应分别提供示例。

## 6. 兼容性与迁移

- 兼容性影响：待确认
- 调用方处理：待确认

## 7. 待确认

- 待确认的字段、规则、响应、错误码、兼容性影响统一列在这里。

> 生成交付文档时，删除没有内容的条目和章节；本模板中的占位符、说明文字及示例只用于生成，不得原样保留。交付文档只描述当前最终状态，不记录来源、分析、讨论、修订或其他调整过程。
